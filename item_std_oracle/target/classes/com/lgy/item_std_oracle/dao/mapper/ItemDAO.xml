<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lgy.item_std_oracle.dao.ItemDAO">
    <select id="list" resultType="com.lgy.item_std_oracle.dto.ItemDTO">
        select name, price, description from mvc_product
    </select>
    <insert id="write">
        insert into mvc_product(name, price, description) values (#{name}, #{price}, #{description})
    </insert>
</mapper>